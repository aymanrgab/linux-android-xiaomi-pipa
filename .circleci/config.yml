version: 2.1

# Droidian kernel packages use the droidian-buildd orb
# which handles building on the Droidian build infrastructure.

setup: true

orbs:
  continuation: circleci/continuation@0.1.2
  droidian-buildd: droidian-releng/droidian-buildd-orb@volatile

jobs:
  setup:
    executor: continuation/default
    resource_class: large
    steps:
      - droidian-buildd/checkout
      - droidian-buildd/generate
      - continuation/continue:
          configuration_path: generated_config.yml

workflows:
  setup:
    jobs:
      - setup:
          filters:
            tags:
              only: /^droidian\/.*\/.*/
